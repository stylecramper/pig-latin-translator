import { PiglatinPipePipe } from './piglatin-pipe.pipe';

describe('PiglatinPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PiglatinPipePipe();
    expect(pipe).toBeTruthy();
  });
});
